{
  "name": "model-thrift",
  "version": "0.3.2",
  "description": "Think with the expensive model, execute with cheaper helpers: an always-on delegation rule plus a guard that rejects subagents launched without an explicit model."
}
